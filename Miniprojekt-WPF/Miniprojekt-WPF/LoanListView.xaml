<UserControl x:Class="Miniprojekt_WPF.LoanListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Miniprojekt_WPF"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="ListStyles.xaml"/>
    </UserControl.Resources>
    <Grid>
        <ListView ItemsSource="{Binding LoanList}" AlternationCount="2">
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="User" Width="280" DisplayMemberBinding="{Binding Customer.Name}" />
                    <GridViewColumn Header="Gadget" Width="280" DisplayMemberBinding="{Binding Gadget.Name}"/>
                    <GridViewColumn Header="Pickup Date" Width="100" DisplayMemberBinding="{Binding PickupDateString}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
