<UserControl x:Class="Miniprojekt_WPF.GadgetListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Miniprojekt_WPF"
             mc:Ignorable="d" d:DesignWidth="300" Height="280">
    <StackPanel>
        <ListView SelectedItem="{Binding SelectedGadget}" ItemsSource="{Binding GadgetList}" Height="235">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Delete" Command="{Binding GadgetDeleteCommand}"  />
        <Button Content="Add" Command="{Binding GadgetAddCommand}" />
        <Button Content="Edit" Command="{Binding GadgetEditCommand}" />
    </StackPanel>
</UserControl>
